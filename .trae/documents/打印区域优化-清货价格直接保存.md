# 打印区域优化 + 清货价格弹窗直接保存

## 需求
1. **LabelPrintCard 移除清货打印按钮**，只剩 3 个按钮：普通标签、折扣标签、大标签
2. **3 个按钮一行显示**（不换行）
3. **清货价格弹窗确认后直接保存**（而非仅更新本地状态）

## 实现步骤

### 步骤 1：修改 `LabelPrintCard` 组件
**文件**: `HbwebExpoApp/src/components/product-maintenance/LabelPrintCard.tsx`

- 移除清货打印按钮相关的 props：`isPrintingClearance`、`canPrintClearance`、`onPrintClearance`
- 移除清货打印按钮 JSX
- 修改 `actions` 样式：移除 `flexWrap: "wrap"`，确保 3 个按钮一行显示

### 步骤 2：修改 `StoreClearancePriceCard` 组件
**文件**: `HbwebExpoApp/src/components/product-maintenance/StoreClearancePriceCard.tsx`

- 移除打印按钮相关 props：`isPrintingClearance`、`onPrintClearance`
- 移除打印按钮 JSX
- 只保留条码和价格区域的一行布局

### 步骤 3：修改 `product-query.tsx`
**文件**: `HbwebExpoApp/app/(tabs)/product-query.tsx`

- **LabelPrintCard 调用**：移除 `isPrintingClearance`、`canPrintClearance`、`onPrintClearance` props
- **StoreClearancePriceCard 调用**：移除 `isPrintingClearance`、`onPrintClearance` props
- **清货价格弹窗直接保存**：修改 `openClearancePriceEditor` 中的 `onConfirmValue` 回调，从仅 `setClearancePriceInput` 改为先 `setClearancePriceInput` 再调用 `handleSaveClearancePrice`

### 步骤 4：验证 TypeScript 编译通过
